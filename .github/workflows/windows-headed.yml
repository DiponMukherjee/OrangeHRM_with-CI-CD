<<<<<<< HEAD:.github/workflows/windows-headed.yml
name: Windows-Headed Test
=======
name: Ubuntu- Headed
>>>>>>> b730ea0ac851ab0db75f52df4c8ac4eac2d5f952:.github/workflows/ubuntu-headless.yml

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      # Enables headed mode by creating a virtual display
      - name: Start Xvfb (Virtual Display)
        run: |
          echo "Starting virtual display..."
          sudo apt install xvfb -y
          Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
          export DISPLAY=:99

      # Optional for Chrome-based tests
      - name: Install Chrome Browser
        uses: browser-actions/setup-chrome@v1

      - name: Install Playwright Browsers
        run: |
          python -m playwright install

      - name: Run Tests in Headed Mode
        env:
          HEADLESS: "false"
        run: |
          pytest
